# Handoff: Step 04(M6 已起草)→ Step 04(下一目标 = M3 `agent-rs/tiktok-p1`)

## 状态

- `plans/modules/m6-once-guard.md` ✅ 已起草(5 任务 M6-T1~T5;03-split 点名的 M6 全部账本 ID 已认领,见该文件 §5)。
- 下一 Step 04 调用:起草 **M3 `agent-rs/tiktok-p1`**(`plans/modules/m3-tiktok-p1.md`),仓库回到本仓 `glance_mind_agent_rs`。
- 之后顺序:M4 → M5 → ROOT(03-split §2;M3/M4 互相独立可并行起草/执行)。

## M3 必须按此消费的既定契约与裁决(不重开)

1. **D-01 限定(04-adjudications.md)**:facebook 保留手写循环是**特例豁免**;**M3 的 tiktok 新循环必须接 M1 `PaginationLoop`**(tikhub 适配器现状为单次调用,无既有循环资产,豁免理由不可迁移)。`accept_page` 驱动、`shortfall_for` 映射、`StopReason` 词汇全套消费 M1 §2 D2。
2. **D1 消费**:tikhub 适配器 override `fetch_by_keyword_with_outcome`(M1 §2 D1);零进展错误走 `Err`,有进展失败走 `PartialFailure`(M2 计划 M2-T2 测试 5/6 形状为样板)。
3. **D-02/D4**:tiktok 上游**有**单页 count 参数且硬上限 20(tikhub/types.rs:297)——`page_size_hint` 在 tiktok **可下发**:hint 作单页请求 count(clamp 20),`options.count` = 总量(R-001 tiktok 行,删 tiktok.rs:96 cap);offset 翻页 `offset += 实际返回条数或单页大小`(以 T-051 实测语义为准),`has_more=0` → 归一化为「cursor 缺失」语义喂 PaginationLoop。
4. **D-04 冻结表**:platform_page_cap tiktok=20;修订须经 root + 上游实测证据。
5. **M2 样板(软依赖)**:任务结构(strategy 解 cap → adapter 接线 → real gate → 收尾)、mock-HTTP 模式、AG-006 金丝雀程序、fixture 回灌控制,均沿 `plans/modules/m2-facebook-p0.md` 形状;tiktok mock helper 按 FR-003 在模块内复制(tikhub/client.rs:1788 已有同构 helper)——**注意:若 M3 构成第三次 helper 复制,按 M2 §6.4 登记 root 裁决是否提升至 `src/testing`**。

## M3 起草必读清单(仅此清单)

计划族文件:
1. `00-manifest.md`
2. `03-split.md`(M3 行:§2/§3/§5/§6)
3. `04-adjudications.md`(D-01/D-02/D-04 对 M3 的绑定指令)
4. `plans/modules/m1-pagination-core.md` §2(D1~D4 = 消费契约)
5. `plans/modules/m2-facebook-p0.md` §1/§3 任务模板 + §6(样板与已决裁决)
6. `constraints/testing-constraints.md`
7. Ledger 相关行(只读 M3 相关行):
   - requirements:R-001(tiktok 行)、R-003;R-012 约束
   - test-suite:T-001(tiktok 行)、T-011、T-051
   - providers:PV-002(offset 递进断言 + fixture 扩展第二页)
   - production-dependencies:P-002(`TIKHUB_API_KEY` gate、≤3 调用、只读)
   - invariants-failures:I-001~I-004(实例化证据侧)、F-001/F-002/F-004/F-005(tiktok 实例)
   - anti-gaming:AG-001~AG-007、AG-010~AG-012(全局)、AG-020~AG-023(实例化引用)
   - framework-test-research:FR-003(helper 复制裁决)

源码(M3 owner surface,03-split §6):
- `src/strategies/tiktok.rs:96`(cap 行)
- `src/adapters/tikhub.rs:186-187`(单次调用现状 = 翻页循环落点)
- `src/tikhub/client.rs:274`(fetch_video_search_result)、`:1788`(mock helper 样板)
- `src/tikhub/types.rs:25-26`(请求 offset/count)、`:297,301-304`(单页≤20 硬约束、has_more/cursor 响应字段)
- `tests/real_api_test.rs:3-4`(`TIKHUB_API_KEY` gate 既有约定)
- `tests/fixtures/tiktok/search_travel_us.json`(既有真实样本;第二页 fixture 须取自 T-051 实跑回灌,PV-002)

## M3 起草时必须携带的裁决(防漂移)

- **M3 覆盖 ID(03-split §3,须全部认领)**:R-001(tiktok)/R-003、T-001(tiktok)/T-011/T-051、PV-002、P-002;I-001~I-004/F-x 为实例化证据侧;AG 全局继承。
- T-011 断言要点(test-suite):offset 在请求间**递进**、`has_more=0` 停止、达量停止;mock-HTTP 捕获请求序列断言 offset 序列(0,20,40 形状)。
- real gate T-051(P-002):`TIKHUB_API_KEY`、≤3 调用、只读;通过证据 = 第二页与首页内容差异 + has_more/cursor 字段**实测值**回灌 fixture。
- 每任务硬规格同前:RED+预期失败信息、GREEN 命令、验收命令、「实现者不得修改断言」声明、测试/实现分上下文(AG-004);收尾任务含 AG-012 预检 + R-012 diff 自查。
- M2 已决事项不得重开:循环形态问题对 M3 不存在(必须 PaginationLoop,D-01);先绿类测试须 AG-006 证明。

## 开放事项(全族,对 M3 的影响)

- root 待办池(不阻塞 M3 起草):D-10(live 测试 env-skip 修复)、M2 §6.4/本 handoff §「helper 第三次复制」裁决、T4 mutation CI required-check 设置(M6 §6.3)。
- V1 仍 OPEN,仅 gate M5;对 M3 无影响。
