# Handoff: Step 02 → Step 03(Split / Module Map)

## 下一步

`03-split` —— 基于账本把范围拆为模块图(模块边界、依赖序、每模块引用的 R-/I-/T-/C-/P-/PV-/AG-/FR- 条目),产出 `03-split.md`;manifest 模块队列(pagination-core / platform-strategies / once-guard)为初步输入,Step 03 最终定。

## 必读文件(仅此清单;per step-02 规范)

1. `00-manifest.md`
2. `01-first-principles.md`
3. `ledgers/requirements.md`(R-001~R-013 + V1)
4. `ledgers/invariants-failures.md`(I-001~I-009、F-001~F-009、横切裁决)
5. `ledgers/test-suite.md`(T-001~T-054、justified gaps)
6. `ledgers/anti-gaming-test-quality.md`(AG-001~AG-024;Step 04 任务起草的强制引用源)
7. `ledgers/production-dependencies.md`(P-001~P-007)
8. `ledgers/providers.md`(PV-001~PV-005)
9. `ledgers/cross-service-contracts.md`(C-001~C-005;O1 闭环证据)
10. `ledgers/framework-test-research.md`(FR-001~FR-005)
11. `ledgers/non-code-exceptions.md`(N-001~N-004)
12. `ledgers/llm-api-boundary.md`(INAPPLICABLE 裁决 + 重评触发条件)
13. `ledgers/assumptions.md`(含 Step 02 增补)
14. `00-context-inventory.md`(仅作文件路径查找,不重读结论)

## 拆分时必须携带的裁决(防漂移)

- 模块边界跨两 git 仓:scheduler 模块(once-guard)独立提交流程;agent-rs 两模块同仓。
- P0=facebook,P1=tiktok/reddit/twitter,P2=instagram(gated on V1/C-005;只阻塞分支选择)。
- A005 已 drop:trait 不传 cursor;fetch 路径只暴露欠交付原因(R-007)。
- 预算/schema/Redis 形状零改动(R-012、C-001、契约不变约束 §3)。
- proptest 引入(FR-001)归 pagination-core 模块的前置任务;scheduler 模块必须含本地 cargo-mutants 证据要求(AG-013/FR-002)。
- C-004 是本计划唯一新增跨服务读依赖(scheduler 读 terminal_reason),拆分时该接线任务须同时出现在两侧模块的依赖说明里。

## ID 约定(traceability 编译引用)

R-xxx(需求)/ I-xxx(不变量)/ F-xxx(失败模式)/ T-xxx(测试)/ AG-xxx(反作弊规则)/ P-xxx(生产依赖)/ PV-xxx(provider)/ C-xxx(跨服务契约)/ FR-xxx(框架研究)/ N-xxx(非代码核查)/ V1(验证任务)/ A-xxx·B-x(假设/bedrock)。ID 一经发布不复用、不重编号。

## 开放阻塞

- 无 P0/P1 流程阻塞。O1 已闭环(C-003);V1 仍 OPEN 但仅 gate instagram 分支(C-005/N-001)。
- 两个 Step 04 裁决预告(非 Step 03 阻塞):① R-010 防御强度「DECIDED-推荐」待评审确认;② P-007 补偿控制形态(最小 gated 测试 vs N-003 人工 SQL)。

## Step 02 验证证据

- step-02 规范的 9 类账本全部落盘(llm-api-boundary 为带证据的 INAPPLICABLE;non-code-exceptions 4 条,其中 N-004 已完成)。
- 每条需求(R-001~R-013)均映射 ≥1 种验收证据;每个 feature 均有确定性 mock 测试或非代码例外;每个真实依赖有 real gate 或证据型 inapplicability(P-006 部分、P-007 带补偿控制);provider 行为均有 mock+real 双 gate;变异门槛(AG-010~AG-013)与属性测试(PT-1~PT-5,scheduler 豁免带理由)齐备。
- O1 读方枚举完成并闭环(cross-service-contracts.md §2,五仓 grep 实证;gm-e2e 为唯一取值敏感读方且天然兼容 `SEARCH_EXHAUSTED`)。
- 账本期新发现已增补进 assumptions.md(proptest 缺失、scheduler 无 mutation CI、C-004 新跨服务读依赖)。
