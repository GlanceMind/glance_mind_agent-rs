# Handoff: Step 04(M2 已起草)→ Step 04(下一目标 = M6 `scheduler/once-guard`)

## 状态

- `plans/modules/m2-facebook-p0.md` ✅ 已起草(7 任务 M2-T1~T7;handoff 04-m1-to-04-m2 点名的 M2 全部账本 ID 已认领,见该文件 §7 覆盖自查)。
- 下一 Step 04 调用:起草 **M6 `scheduler/once-guard`**(`plans/modules/m6-once-guard.md`)。
- **注意:M6 的 owner surface 在兄弟仓** `/Users/jacksoom/programer/aihub/glance_mind_worker/glance_mind_scheduler`(独立 git 仓、独立提交流程);模块计划文件仍写在本计划族目录。
- 之后顺序不变:M3 → M4 → M5 → ROOT(03-split §2)。

## M1/M2 已定形、M6 必须按此消费的契约(待 root 冻结,起草期按各计划引用)

1. **C-004 terminal_reason 读方契约(M6 的核心输入)**:agent 写方值集不变,6 个 code 字符串已由 M1-T6 契约 pin:`COMPLETED` / `COMPLETED_WITH_PARTIAL_ERRORS` / `NO_MORE_POSSIBLE_DATA` / `PROVIDER_FAILURE` / `CANCELLED` / `INTERNAL_ERROR`;持久化形状 `"CODE: message"`(`as_terminal_message`,以 `:` 分隔取首段得 code)。M6 新增读取 `gm_crawler_tasks.terminal_reason`(db.rs 加查询),**必须容忍 NULL/未知值**(滚动部署期新旧 agent 并存,契约不变约束 §3.3)。
2. **M1 D3 裁决(M6 的语义前提)**:agent 在「有进展 + 上游枯竭」时**不**调 `stop_campaign_gracefully`,只写 terminal_reason=NO_MORE_POSSIBLE_DATA —— ONCE campaign 的完结理由区分**完全由 M6 消费 terminal_reason 实现**。M2-T4.1 已断言 agent 侧不停 campaign(集成证据)。
3. **T-040「无 WARN」半边归 M6**:T-040 账本行的「50 条/COMPLETED/无 WARN」中,「无 WARN」是 scheduler 侧断言 —— M6 的 T-022(扫满 → 无 WARN、ONCE_EXECUTED 不变)必须认领此半边(M2 计划 §4 T5 注已声明此边界)。
4. **C-001/C-002(M6 引用为「不变」前提)**:scheduler 写方(`dispatch_task`,lib.rs:313-383)零改动;`search_limit`/`search_offset` 语义注释 scheduler 侧落地 = N-002 scheduler 半边(M6 任务)。
5. **eval_once failed 一次重派语义不得破坏**(R-008/A007,schedule_evaluator.rs:119-127):M6 的防御分支不得改变 failed 重派行为。

## M6 起草必读清单(仅此清单)

计划族文件(本仓 `docs/plans/jplan/2026-06-10-pagination-max-scan/`):
1. `00-manifest.md`
2. `03-split.md`(M6 行:§2/§3/§5/§6;完成判据 §5 M6 行)
3. `constraints/testing-constraints.md`(scheduler 仓同等适用,见其末行)
4. `handoffs/03-to-04.md`(M6 裁决预告节:R-010 强度、P-007 形态、completed_reason 终名、AG-013、C-004 双侧呼应)
5. `plans/modules/m1-pagination-core.md` §2 D3/D5 + M1-T6(terminal_reason code pin = M6 读方契约)
6. `plans/modules/m2-facebook-p0.md` §4 M2-T5(T-040 边界注)+ §6.2(D3 裁决继承)
7. Ledger 相关行(只读 M6 相关行):
   - requirements:R-010、R-011(scheduler 侧)、R-013;R-008(重派语义保护语境)
   - invariants-failures:I-007;F-008;F-003(区分值语境)
   - test-suite:T-020、T-021、T-022(允许先绿 + AG-006);§3 scheduler 单测模式
   - anti-gaming:AG-001~AG-007(全局)、**AG-013**(scheduler 仓本地变异证据,该仓无 mutation CI)、§3 末(eval_once 属性测试豁免:输入域有限已穷举)
   - production-dependencies:P-007(补偿控制形态 Step 04 定:最小 gated 测试 vs N-003 人工 SQL)
   - framework-test-research:FR-002(scheduler 仓 CI 缺口实证;是否补 CI 由评审定,推荐补)、FR-004(test_schedule_evaluator.rs 模式)
   - cross-service-contracts:C-003(**含 §2 读方枚举证据 + 子串约束**)、C-004(scheduler 读方)、§3 契约不变约束全四条
   - non-code-exceptions:N-002(scheduler 侧注释)、N-003(人工 SQL 验证 fallback)

源码(M6 owner surface,**兄弟仓** `/Users/jacksoom/programer/aihub/glance_mind_worker/glance_mind_scheduler`,行号为 2026-06-10 工作区状态):
- `src/schedule_evaluator.rs:86-151`(eval_once 决策;119-127 failed 重派)
- `src/lib.rs:166-177`(mark_campaign_completed 调用)、`src/lib.rs:313-383`(dispatch_task,写方零改动参照;354-355 search_limit/search_offset 写入)
- `src/db.rs:66-77,113-131`(campaign 完结写入;+ 新增 terminal_reason 读取点)
- `src/test_schedule_evaluator.rs`(确定性表驱动单测模式,T-020~T-022 载体)

## M6 起草时必须携带的裁决(防漂移;**用户已裁决项见 `04-adjudications.md`,以下为输入事实而非开放问题**)

- **M6 覆盖 ID(03-split §3,须全部认领)**:R-010 / R-011(scheduler 侧)/ R-013、I-007、F-008、T-020 / T-021 / T-022、C-003 / C-004(scheduler 侧)、P-007、FR-002 / FR-004、N-002(scheduler 侧)/ N-003、AG-013;AG-001~AG-007 全局继承。
- **R-010 防御强度 = WARN 日志 + 指标,不自动补派(已裁决 D-08,功能理由:防预算重复消耗与补派循环)**;保留升级扩展点。I-007:防御分支纯观测,零状态改变、零派发 —— T-020 须双向断言(WARN 决策产生 + 状态机/派发不变)。
- **R-013 completed_reason 终名 = `SEARCH_EXHAUSTED`(已裁决 D-06)**:含 `EXHAUST` 子串直接通过 gm-e2e 白名单(C-003,test_10_status_transitions.py:215-219),零跨仓断言改动;M6 计划直接以此值写任务与断言。
- **F-008 双向断言(T-021)**:枯竭欠扫(terminal_reason=NO_MORE_POSSIBLE_DATA)→ 不告警 + completed_reason=`SEARCH_EXHAUSTED`;非枯竭欠扫 → WARN。
- **T-022 允许先绿**:须经 AG-006 变异证明有效(AG-013 预检中对应代码被变异时变红),否则无效证据。
- **AG-013 + mutation CI(已裁决 D-09)**:M6 计划必须含**两件**:① 收尾任务:仓内 `git diff main...HEAD > /tmp/pr.diff && cargo mutants --in-diff /tmp/pr.diff -- --test-threads=1` 无 missed + 输出留存(CI 落地前过渡证据);② **独立任务:为 `glance_mind_worker` 仓新增 scheduler crate 的 mutation 工作流**(镜像 agent-rs `.github/workflows/mutation-rust.yml`,pinned cargo-mutants 24.11.0,PR `--in-diff`)。
- **P-007 补偿控制 = scheduler 仓新建最小 `DATABASE_URL`-gated 测试(已裁决 D-07)**:断言 `mark_campaign_completed` 写 `SEARCH_EXHAUSTED` 落库可查;N-003 人工 SQL 降级为部署后可选抽查(运维剧本步骤,非验收必需)。
- **属性测试豁免**:eval_once 输入域有限(status × process_count × terminal_reason),T-020~T-022 表驱动穷举即可(anti-gaming §3 末),M6 不引入 proptest。
- **跨仓滚动部署兼容**:scheduler 读 terminal_reason 容忍 NULL/未知值;agent/scheduler 部署无硬顺序(03-split M6 行)。
- 测试硬规则同本仓:每任务 RED 测试 + 预期失败信息 + GREEN 命令 + 验收命令 + 「实现者不得修改断言」声明 + 测试/实现分上下文(AG-004)。

## 开放事项状态(2026-06-10 用户裁决后,见 `04-adjudications.md`)

1. ~~M1 §6.1 / M2 §6.2:Exhausted(有进展)不停 campaign~~ —— **已裁决采纳(D-03)**:M6 的完结理由区分以此为既定前提;Step 06 评审否决须附功能性反证并经用户确认。
2. ~~M1 §6.2:platform_page_cap 冻结表~~ —— **已冻结(D-04)**;对 M6 无影响。
3. ~~M2 §6.1:facebook 保留手写循环~~ —— **已裁决采纳(D-01)**;注意 D-01 为 facebook 特例豁免,**M3~M5 新循环仍按 PaginationLoop 接入**;对 M6 无影响。
4. ~~M2 §6.3:page_size_hint fb 豁免~~ —— **已裁决采纳(D-02)**,root 冻结 D4 时写入措辞;对 M6 无影响。
5. V1 仍 OPEN(仅 gate M5,D-12 维持);对 M6 无影响。
6. 新增(D-10):facebook_real_api_test env-skip 守卫修复立项为 **root 阶段独立测试基建任务**;对 M6 无影响。
