# Handoff: Step 01 → Step 02(Ledgers)

## 下一步

`02-ledgers` —— 基于第一性归约结果构建规划账本(需求账本、契约账本、风险账本等,按 jplan step-02 参考文件)。

## 必读文件(仅此六个 + step-02 参考)

1. `00-manifest.md`
2. `00-context-inventory.md`
3. `constraints/testing-constraints.md`
4. `01-first-principles.md`(平台分页能力事实表 §3、六问裁决 §5、Reconstruction §7 是账本主输入)
5. `ledgers/assumptions.md`(已建,Step 02 起只增不改;含 V1 验证任务与 O1 OPEN 项)
6. `~/.claude/skills/jplan/references/step-02-ledgers.md`(step-02 权威流程)

## 可选源码(仅在构建契约/需求账本需要逐字段引用时读)

- `src/ports/content_gateway.rs`(trait 现签名,A005 的契约缺口)
- `src/ports/progress_tracker.rs`(TaskTerminalReason 全集,L60-98)
- `src/strategies/{facebook,tiktok,reddit,twitter,instagram}.rs`(各 cap 行:131/96/100/123/99)
- `src/adapters/facebook.rs`(L522-644 既有翻页构造,作为模式样板)
- `src/adapters/redis.rs`(L449-465 task→TaskConfig 字段映射)
- `../glance_mind_worker/glance_mind_scheduler/src/schedule_evaluator.rs`(eval_once L86-151)
- `../glance_mind_worker/glance_mind_scheduler/src/lib.rs`(dispatch_task L313-383)

## 避免读取

- `mutants.out/`、`e2e/`、`src/adapters/postgres.rs` 全文(只需知道 terminal_reason 持久化在 L2123-2181)、计划族空目录(plans/、reviews/、patches/、compile/)。

## 开放阻塞

- 无 P0/P1 流程阻塞。
- **V1**(Instagram 请求端分页能力验证):已是计划内验证任务,带方法与判定标准(assumptions.md「计划内验证任务」表);排期在 Step 04,不阻塞 Step 02。
- **O1**(scheduler `completed_reason` 枚举值):OPEN,Step 02 必须将其登记进跨服务契约账本,并列出「读方枚举」信息收集任务。

## Step 01 验证证据

- 平台分页能力 6 项核查全部落地为带行号的事实表(01 §3),其中 4 平台代码判定、1 平台(instagram)转为计划内验证任务 V1 —— **无 ASSUMED 残留**。
- assumptions.md 共 5 BEDROCK + 12 条裁决,全部为 keep/keep-justified/drop,无 ASSUMED。
- 关键归约成果:A005「trait 扩展 cursor/has_more」被 drop,最小契约缺口改写为「fetch 路径暴露欠交付原因」;manifest 模块队列描述已同步更新。
- 重大事实修正:Facebook 适配器**已实现** cursor 翻页(mock 测试在 facebook.rs:1169-1203),事故根因收敛为 strategy 总量截断(facebook.rs:131)+ 欠交付原因缺失 + scheduler 无防御校验。
- Gate 适用性:mock/real/property/mutation/cross-service 均 REQUIRED;db-migration-guard 与 api-contract-guard 标记 INAPPLICABLE(带证据,01 §7)。
- manifest 唯一 next_step = `02-ledgers`;本 handoff 所列文件均真实存在。
