# Handoff: Step 04(M1 已起草)→ Step 04(下一目标 = M2 `agent-rs/facebook-p0`)

## 状态

- `plans/modules/m1-pagination-core.md` ✅ 已起草(7 任务,M1 全部账本 ID 已认领,见该文件 §5)。
- 下一 Step 04 调用:起草 **M2 `agent-rs/facebook-p0`**(`plans/modules/m2-facebook-p0.md`)。
- 之后顺序不变:M6 → M3 → M4 → M5 → ROOT(03-split §2)。

## M1 已定形、M2 必须按此消费的共享接口(待 root 冻结,起草期按 M1 计划 §2 引用)

1. **D1 欠交付契约**:`FetchShortfall::{Exhausted, PartialFailure{message}}` + `FetchOutcome{contents, shortfall}`;trait 默认方法 `fetch_by_keyword_with_outcome`(默认 shortfall=None)。M2 任务 = facebook 适配器 override 此方法。
2. **D2 翻页状态机**:`src/pagination.rs` `PaginationLoop`(`accept_page(&[String], Option<String>) -> PageOutcome{newly_accepted, decision}`、`StopReason::{ReachedMaxCount, UpstreamExhausted, CursorLoop, EmptyPageLimit}`、`shortfall_for`、`MAX_EMPTY_PAGES=3`)。M2 把 facebook.rs:522-644 既有手写循环改接共享状态机(或保持既有循环、补 shortfall 接线 —— 形态由 M2 计划裁决并说明,但欠交付原因必须经 D1 上报)。
3. **D3 orchestrator 映射**:Exhausted→NO_MORE_POSSIBLE_DATA;PartialFailure→COMPLETED_WITH_PARTIAL_ERRORS;shortfall=None→现状;零进展错误→failed(M1-T4 已落)。M2 不得在 orchestrator 重复实现,只提供集成证据(T-015/T-016/T-017 实例化)。
4. **D4 页大小语义**:`TaskConfig.page_size_hint: Option<u32>`(redis 映射写入,clamp 到 `platform_page_cap`,facebook=20);M2 的 strategy 任务消费此字段(`build_search_options` 用 hint 作单页 count、`options.count` 改传总量 max_videos —— R-001 facebook 行,删 `v.min(20)` 截断,strategies/facebook.rs:131)。
5. **MockContentGateway 分页注入**(M1-T3):`add_search_pages` / `set_page_error_at` —— M2 的 orchestrator 级集成测试可直接复用;facebook 适配器级测试用既有 mock-HTTP 样板(facebook.rs:1169-1203、1269-1311)。

## M2 起草必读清单(仅此清单)

计划族文件:
1. `00-manifest.md`
2. `03-split.md`(M2 行:§2/§3/§5/§6)
3. `plans/modules/m1-pagination-core.md` **§2(共享接口)+ §3 任务边界**(避免重复认领)
4. `constraints/testing-constraints.md`
5. `handoffs/03-to-04.md`(防漂移裁决节,仍生效)
6. Ledger 相关行(只读 M2 相关行):
   - requirements:R-001(fb 行)、R-002;R-012 约束
   - invariants-failures:I-006;F-001~F-006、F-009(证据侧)
   - test-suite:T-001(fb)、T-010、T-015、T-016、T-017、T-040、T-050、T-054
   - anti-gaming:AG-001~AG-007、AG-010~AG-012(全局)、AG-020~AG-024(引用适用性)
   - production-dependencies:P-001、P-005
   - providers.md:PV-001(真实输出回灌 fixture 控制)
   - framework-test-research:FR-003(mock 样板扩展)、FR-005
   - cross-service-contracts:§3 契约不变约束

源码(M2 owner surface,03-split §6):
- `src/strategies/facebook.rs:131,238`(cap 行)
- `src/adapters/facebook.rs`(522-644 循环;569-576 RateLimited 先例;1169-1203/1269-1311 mock-HTTP 样板)
- `tests/facebook_real_api_test.rs:54-58`(凭据 gate 模式)
- `tests/facebook_real_db_test.rs:278-285`(real-DB gate 模式)
- 参照(只读):`src/pagination.rs` / `src/ports/content_gateway.rs` 的 M1 计划形状(执行期为实码)

## M2 起草时必须携带的裁决(防漂移)

- M2 覆盖 ID(03-split §3,须全部认领):R-001(fb)/R-002、T-001(fb)/T-010/T-015/T-016/T-017/T-040/T-050/T-054、PV-001、P-001/P-005、F-001~F-006(注入/集成证据侧)、F-009(T-054 证据)、I-006(real-DB 守恒 + root 共担)。
- T-040 事故重演(campaign 269 形状)是 root chain gate 的最终验收项,M2 计划必须给出 mock 层两形状(上游充足→50/COMPLETED;上游仅 20→NO_MORE_POSSIBLE_DATA)。
- real gates(T-050/T-054)须含:凭据 env、调用次数上限(P-001 ≤3 次)、幂等/清理、AG-007 live 漂移区分;不入默认 `cargo test`。
- 真实输出回灌 fixture(PV-001):新 fixture 必须取自 T-050 实跑输出,禁止凭空捏造字段。
- M1 的「允许先绿」清单与 AG-006 证明义务模式可复用(M1 计划 §3 各任务模板)。

## 开放事项(M1 起草期产生,交 root/评审)

1. M1 计划 §6.1:Exhausted(有进展)不调 `stop_campaign_gracefully` 的设计裁决,待评审确认。
2. M1 计划 §6.2:platform_page_cap 冻结表(fb=20/tiktok=20/reddit=100/twitter=100/ig=50/unknown=20),M2~M5 修订须经 root。
3. M1 计划 §6.3:PT-5 变异豁免预案。
4. V1 仍 OPEN(仅 gate M5);对 M2 无影响。
