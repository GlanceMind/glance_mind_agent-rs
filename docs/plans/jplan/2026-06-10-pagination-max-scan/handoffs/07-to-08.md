# Handoff: Step 07(patch-iterate 完成)→ Step 08(Traceability Compile)

## 状态

- **Step 07 完成**:6 个补丁批次(A=M1、B=账本族、C=M2、D=M6、E=root、F=M3/M4/M5)全部按 subagent-driven-development 流程执行(独立实现者 + spec 合规审 + 质量审两段评审,返修闭环);patch notes 齐于 `patches/07-batch{A..F}-*.md`。
- **P1×8 + P2×14 + P3 批全部闭合**:每条的改动与闭合理由见对应 patch note;无 P0/P1 遗留、无降级、无延迟。
- **执行期新事实(批 F 现状核对)**:main 已合 PR #5(tiktok 独立修复,绕开 M1 契约)——M3 已改写为「迁移到共享契约」,差异清单与既有测试撞红处置路径(f3/r6a → ASSERTION-CHANGE-JUSTIFIED,测试上下文执行)已入计划。
- 计划族已入 git:分支 `docs/jplan-pagination-max-scan`,PR #6(https://github.com/GlanceMind/glance_mind_agent-rs/pull/6);Step 07 收口为第二个 commit。

## Step 08 必读(仅此清单)

1. `00-manifest.md`
2. `~/.claude/skills/jplan/references/step-08-traceability.md` + `references/reviewer-prompts.md` 末节「Traceability Compiler」(PASS/FAIL 清单)
3. `plans/root.md` + `plans/modules/m{1,2,3,4,5,6}-*.md`(补丁后现行版)
4. `03-split.md`(覆盖映射基准)+ 全部 `ledgers/*.md`(补丁后现行版)
5. `04-adjudications.md`(D-01~D-15)
6. `patches/07-batch*.md`(closure rationale 抽查源)

## Compiler 须知(Step 07 引入的校验要点)

- 「允许先绿」类别现在有三种合法形态:AG-006 变异覆盖(diff 内代码)、AG-006 手工金丝雀(零改动代码,程序逐条写明)、AG-008 探针类(live gated,三判据)——清单在各计划 §5/完成判据,逐条核归属。
- M3 的 RED 重分类(PR #5 消解项)与「既有测试处置段」是有意设计,非缺口;per-request count 语义标「待执行期复核」是显式开放项,非 placeholder。
- D3 映射表 = 六行(M1 §2 现行);D1 构造不变量、DR-10 触发集、empty_streak 语义为新增冻结段,下游引用一致性已经批 C/D/E/F 同步。
- 非代码例外 = N-001~N-007(全部入账);R-001 五平台行、F-010 等新增行的 owner 映射以各计划 §5 现行表为准。

## 开放项(不阻塞 compile,如实呈报)

- V1(M5-T1 执行期判定);M3 per-request count 上游行为(T-051 实测);M6 required-check 手工设置(N-006);RT-2.1 helper 提升 backlog;user-videos 双循环归并 backlog(M3 §2.2)。
