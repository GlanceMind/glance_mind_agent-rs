# Handoff: Step 03 → Step 04(Draft Module Plans)

## 下一步

`04-draft` —— 按定稿顺序逐个起草 module plan(一次 Step 04 调用起草一个),产出文件名见 `03-split.md` §6 文件面表(`plans/modules/m1-pagination-core.md` 等);全部模块计划完成后起草 `plans/root.md`(root 职责见 `03-split.md` §4)。

## 起草顺序(03-split.md §2 定稿)

1. **M1 `agent-rs/pagination-core`** ← 本次 Step 04 目标
2. M2 `agent-rs/facebook-p0`
3. M6 `scheduler/once-guard`(兄弟仓 `/Users/jacksoom/programer/aihub/glance_mind_worker/glance_mind_scheduler`)
4. M3 `agent-rs/tiktok-p1`
5. M4 `agent-rs/reddit-twitter-p1`
6. M5 `agent-rs/instagram-p2`(首任务 = V1 验证)
7. ROOT `plans/root.md`

## 第一个计划:M1 `agent-rs/pagination-core` 必读清单(仅此清单)

计划族文件:

1. `00-manifest.md`
2. `03-split.md`(M1 的 owner 面、shared 标注、完成判据 §5)
3. `constraints/testing-constraints.md`
4. Ledger 相关行(只读 M1 相关行,不重读全文):
   - `ledgers/requirements.md`:R-007、R-008、R-009、R-011(agent 侧)、R-012;R-001 仅读「解耦机制归 M1」语境
   - `ledgers/invariants-failures.md`:I-001~I-005、I-008、I-009;F-001/F-002(语义层)、F-009(fallback 兼容)
   - `ledgers/test-suite.md`:T-002、T-003、T-004、T-030~T-034;§8 justified gaps
   - `ledgers/anti-gaming-test-quality.md`:AG-001~AG-007(全局)、AG-010~AG-012、AG-020~AG-024(PT 生成器要点)
   - `ledgers/framework-test-research.md`:FR-001(proptest 引入,M1 前置任务,含 ④ proptest-regressions 入库与 mutants 预算)、FR-003(mock 基建)、FR-005
   - `ledgers/cross-service-contracts.md`:C-001、C-002、C-004(agent 侧:不得重命名 TaskTerminalReason 既有序列化字符串)+ §3 契约不变约束
   - `ledgers/production-dependencies.md`:P-006
   - `ledgers/non-code-exceptions.md`:N-002(agent 侧注释)
   - `ledgers/assumptions.md`:A005(drop 裁决——trait 不传 cursor)、A006、A007、B2/B4

源码(M1 owner surface):

- `src/ports/content_gateway.rs`(trait 现返回 `Vec<Content>`,L33-37 = 契约缺口)
- `src/orchestrator.rs`(fetch_content ~L668-682 单次调用;终态映射 ~L412)
- `src/ports/progress_tracker.rs`(TaskTerminalReason L60-98;redaction)
- `src/adapters/postgres.rs`(L2123-2181 terminal_reason 持久化与存储过程缺失 fallback;F-009 兼容面)
- `src/adapters/redis.rs`(to_domain_task_config L438-465)
- `src/domain/entities.rs`(TaskConfig/SearchOptions)
- `src/adapters/facebook.rs`(L522-644 翻页样板,只读参照,不在 M1 改动面)
- `src/testing/mock_gateway.rs`(PT 套件注入载体)
- `Cargo.toml`(FR-001 dev-dep)

## 起草时必须携带的裁决(防漂移)

- A005 已 drop:trait 不传 cursor/has_more;M1 只暴露「欠交付原因」(exhausted / partial-failure),形态由 M1 计划定并经 root 冻结(03-split §4.1)。
- 每个任务必须引用 AG 条目并写明:预期 RED 失败信息(具体到断言文本)、GREEN 命令、验收命令(AG-001);测试任务与实现任务分上下文(AG-004)。
- R-012 约束:不动 schema/预算/Redis 形状;不触发 db-migration-guard。
- proptest:`PROPTEST_CASES` 若下调须写入任务说明,不得以此弱化断言(FR-001 ③);`proptest-regressions/` 入库(FR-001 ④)。

## 后续模块的裁决预告(非 M1 范围,起草到对应模块时处理)

- M6:R-010 防御强度「DECIDED-推荐」待评审确认;P-007 补偿控制形态(最小 gated 测试 vs N-003 人工 SQL);completed_reason 终名(候选 `SEARCH_EXHAUSTED`,须满足 C-003 子串约束);AG-013 本地变异证据;C-004 双侧接线任务须与 M1 侧呼应。
- M5:V1 为首任务(N-001 文档侧 + T-053 探测 ≤4 调用);两分支均预起草,执行期按判定标准选择。
- M2~M5 真实输出回灌 fixture(providers.md 控制)。

## 开放阻塞

- 无 P0/P1 流程阻塞。V1 仍 OPEN,仅 gate M5 分支选择。
