# Handoff: Step 00 → Step 01(First-Principles Reduction)

## 下一步

`01-first-principles` —— 按 `~/.claude/skills/jplan/references/first-principles.md` 对请求做第一性归约:把「翻页修复」还原为不可再分的需求与假设清单,消除所有 `ASSUMED` 项或将其转为待验证事实。

## 必读文件(仅此五个)

1. `00-manifest.md`
2. `00-context-inventory.md`
3. `constraints/testing-constraints.md`
4. `~/.claude/skills/jplan/references/step-01-first-principles.md`
5. `~/.claude/skills/jplan/references/first-principles.md`

## 可选文件(论证需要时才读)

- `src/orchestrator.rs`(fetch_content,~L667)
- `src/strategies/facebook.rs`(L131, L238)
- `src/adapters/redis.rs`(L438-464)
- `../glance_mind_worker/glance_mind_scheduler/src/schedule_evaluator.rs`(eval_once)
- `../glance_mind_worker/glance_mind_scheduler/src/lib.rs`(dispatch_task)

## 避免读取

- 计划族其他子目录(plans/、reviews/、patches/、compile/)—— 尚为空。
- `mutants.out/`、`e2e/`、整仓扫描。

## 开放阻塞项(Step 01 需要逐条裁决「事实/假设/待验证」)

1. Facebook RapidAPI 搜索是否支持 cursor 分页(P1:决定 Facebook 能否扫满 50;若不支持,需要替代策略——多关键词变体、时间窗切分或如实上报 partial)。
2. 其余平台上游分页能力形态(TikHub TikTok 已知有 cursor/has_more)。
3. `gm_crawler_tasks.search_offset`/`search_limit` 字段最终语义。
4. 翻页中途单页失败语义(partial-error 完结 vs 整任务 failed)。
5. 搜索枯竭时 campaign 完结理由(区分 ONCE_EXECUTED / SEARCH_EXHAUSTED)。
6. scheduler 兜底动作强度(仅 WARN vs 允许补派一次)。

## 本步验证证据

- 目录布局已按 SKILL.md 创建(constraints/ ledgers/ plans/modules/ reviews/{autoplan,domain}/ patches/ compile/ handoffs/)。
- `~/.claude/TESTING_CONSTRAINTS.md` 已加载并摘要为 `constraints/testing-constraints.md`(无缺失,无 P1 阻塞)。
- 上下文盘点为路径+一行相关性,未粘贴大段内容;生产证据(campaign 269/task 4285 数值、日志时间线)已固化,后续步骤无需再连生产环境。
- manifest 指定唯一 next_step:`01-first-principles`;Step 01 所需文件全部存在。
